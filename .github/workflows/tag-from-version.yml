# When VERSION changes on the default branch, create and push v{VERSION} if that
# tag does not exist yet. Tag push uses GITHUB_TOKEN; release-windows.yml is then
# started via workflow_dispatch (token pushes do not trigger on: push: tags).
name: Tag from VERSION

on:
  push:
    branches: [main, master]
    paths:
      - "VERSION"
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read and validate VERSION
        id: ver
        run: |
          set -euo pipefail
          if [[ ! -f VERSION ]]; then
            echo "VERSION file missing" >&2
            exit 1
          fi
          raw="$(cat VERSION)"
          v="${raw//$'\r'/}"
          v="${v#"${v%%[![:space:]]*}"}"
          v="${v%"${v##*[![:space:]]}"}"
          if [[ -z "$v" ]]; then
            echo "VERSION is empty" >&2
            exit 1
          fi
          if [[ "$v" =~ [[:space:]] ]]; then
            echo "VERSION must be a single line with no internal whitespace: got '$v'" >&2
            exit 1
          fi
          if [[ ! "$v" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "VERSION must look like 0.1.0 or 0.2.0-beta.1; got '$v'" >&2
            exit 1
          fi
          echo "version=${v}" >> "${GITHUB_OUTPUT}"
          echo "tag_name=v${v}" >> "${GITHUB_OUTPUT}"

      - name: Skip if tag already exists
        id: exists
        run: |
          set -euo pipefail
          tag="${{ steps.ver.outputs.tag_name }}"
          refs="$(git ls-remote --tags origin "refs/tags/${tag}" 2>/dev/null || true)"
          if [[ -n "$refs" ]]; then
            echo "Release tag ${tag} already exists — nothing to do."
            echo "exists=true" >> "${GITHUB_OUTPUT}"
          else
            echo "exists=false" >> "${GITHUB_OUTPUT}"
          fi

      - name: Create and push tag
        if: steps.exists.outputs.exists == 'false'
        env:
          TAG_NAME: ${{ steps.ver.outputs.tag_name }}
        run: |
          set -euo pipefail
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git tag -a "${TAG_NAME}" -m "Release ${TAG_NAME}" "${GITHUB_SHA}"
          git push origin "refs/tags/${TAG_NAME}"

      - name: Start release workflow
        if: steps.exists.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ steps.ver.outputs.tag_name }}
        run: |
          set -euo pipefail
          gh workflow run release-windows.yml --ref "${TAG_NAME}"
